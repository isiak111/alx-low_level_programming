#include <stdio.h>
/**
 * main - a program which prints a aline useing puts function
 * return: 0 (Success) 
 */
int main(void)
{
puts(*\"Programming is like building a multilingual puzzle*);
return (0);
}
